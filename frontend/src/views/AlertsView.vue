<template>
  <div>
    <h1 style="margin-bottom:16px;">Alerts</h1>
    <p style="color:#666;">Alert registration form and existing alerts table will load here.</p>
    <!-- TODO: form (userId, neighborhood, maxPrice, minSize) → POST /api/alerts -->
    <!-- TODO: load existing alerts → GET /api/alerts -->
  </div>
</template>

<script setup>
// POST fetch('/api/alerts', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({...}) })
// GET  fetch('/api/alerts').then(r => r.json()).then(data => { ... })
</script>
